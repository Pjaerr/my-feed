<Collection::Tabs
    @tabs={{this.tabs}}
    @onTabChange={{this.setActiveCollection}} 
    @onTabRefresh={{this.refreshCollections}}
    @activeTab={{this.activeCollection.name}}/>

{{yield this.activeCollection.items}}